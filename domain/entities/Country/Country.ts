export type Country = {
  id: number;
  name: string;
  region: string;
  description: string;
  //we ll use the latitude and longitude to focus the map
  latitude: string;
  longitude: string;
  capital: string;
  numberOfInhabitants: number;
  dateFounded: string;
};
